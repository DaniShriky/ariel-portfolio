VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Server-side only (Project Settings -> API in the Supabase dashboard).
# Never prefix this with VITE_ -- it must not end up in the client bundle.
# Only needed to run scripts/generate-derivatives.ts.
SUPABASE_SERVICE_ROLE_KEY=
