function NodePage() {
  // TODO: walk the node tree using the URL's slug segments,
  // render the matching menu/gallery, or <NotFound /> if a slug doesn't resolve.
  return <div>NodePage placeholder</div>;
}

export default NodePage;
